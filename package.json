{
  "name": "zealer-zone",
  "packageManager": "pnpm@9.2.0",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "ui:add": "pnpm --filter @zealer/ui ui:add"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.2.1",
    "@zealer/eslint-config": "workspace:*",
    "@zealer/typescript-config": "workspace:*",
    "prettier": "^3.3.1",
    "prettier-plugin-tailwindcss": "^0.5.14",
    "turbo": "^2.0.3",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20"
  }
}
